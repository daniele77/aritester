* Login as a modal box? (and where should I put the logout button, then?)
* Add buttons for state query
* Add playback features (retrieve list of available media)
* Improve CSS
* Events detailed inspection

